info:
  name: About us
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseDeliveryApiUrl}}/about-us"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
