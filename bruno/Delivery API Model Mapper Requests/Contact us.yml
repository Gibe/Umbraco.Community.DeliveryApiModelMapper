info:
  name: Contact us
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseDeliveryApiUrl}}item/contact-us"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
